# Shared

Reusable UI blocks that are not tied to a single feature.

- `widgets`: low-level reusable widgets (buttons, inputs).
- `components`: composed UI blocks reused by multiple pages.
- `extensions`: helper extensions for BuildContext and primitives.
