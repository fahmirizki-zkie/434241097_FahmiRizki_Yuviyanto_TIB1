/// Dependency registration entry point.
/// Keep all singleton/factory wiring in one place.
abstract final class InjectionContainer {
  static Future<void> init() async {
    // TODO: Register repositories, use cases, and services.
  }
}
